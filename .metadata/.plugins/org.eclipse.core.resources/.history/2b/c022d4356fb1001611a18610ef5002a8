import java.util.ArrayList;
import java.util.List;
import java.util.TreeSet;

public class gird {
	 public List<Integer> lexicalOrder(int n) {
	        List<Integer> ret = new ArrayList<Integer>();
	        int temp,bao;
	        for(int i = 1;i <=n;i++){
	        	ret.add(i);
	        	temp = i*10;
	        	while(temp <= n){
	        		ret.add(temp);
	        		temp = temp*10;
	        	}
	        	temp = temp/10;
	        	bao = temp;
	        	temp++;
	        	while(temp<= n && (temp/10 - bao/10 )!= 1){
	        		ret.add(temp);
	        		temp++;
	        	}
	        	i = bao/10 ;
	        	if(ret.size() == n)
	        		break;
	        }
	        return ret;
	    }
    public static void main(String args[])
    {
    	gird g = new gird();
    	List<Integer> r = g.lexicalOrder(13);
    	for(int t:r)
    		System.out.println(t);
    }
}